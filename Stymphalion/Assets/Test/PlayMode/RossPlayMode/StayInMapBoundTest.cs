using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;
using UnityEditor;
using UnityEngine.SceneManagement;
/*
public class testPlayer : PlayerClass{
    Vector2 newVelo = new Vector2(1,1);
    private override void Movement(){
        player.velocity = newVelo = newVelo * 2;
    }


}


public class MapBoundTest
{
    [UnityTest]
    public IEnumerator MapNorthTest(){
        SceneManager.LoadScene("Main Island");
        // wiat for scene to load
        yield return new WaitForSeconds(2);

        PlayerClass player = GameObject.Find("Player").GetComponent<PlayerClass>() as testPlayer;

      //  Asset.isTrue((player.location.x > 55) | (player.location.x < -41) | (player.location.y > ))
        
        
    }




}*/